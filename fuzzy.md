این جزوه آموزشی جامع، مقدمه‌ای بر نظریه مجموعه‌های فازی و مفاهیم کلیدی آن ارائه می‌دهد.

# فصل ۱: مقدمه‌ای بر مجموعه‌های فازی

---

### محدودیت نظریه مجموعه‌های کلاسیک

نظریه مجموعه‌های کلاسیک (Crisp Set Theory)، که همه ما با آن آشنا هستیم، بر یک اصل اساسی استوار است: **عضویت دوگانه (Binary Membership)**. در این نظریه، یک عنصر یا به طور کامل عضو یک مجموعه است (عضویت = ۱) یا به طور کامل عضو آن نیست (عضویت = ۰). هیچ حالت بینابینی وجود ندارد.

برای مثال، اگر مجموعه‌ی "اعداد زوج" را در نظر بگیریم، عدد ۴ **قطعاً** عضو این مجموعه است و عدد ۵ **قطعاً** عضو آن نیست.

اما دنیای واقعی به این سادگی نیست. مفاهیمی مانند "جوان"، "بلند قد"، "گرم" یا "نزدیک" مرزهای مشخص و دقیقی ندارند.

- آیا فردی با قد ۱۷۹ سانتی‌متر "بلند قد" است؟
    
- آیا دمای ۲۵ درجه سانتی‌گراد "گرم" محسوب می‌شود؟
    
- مرز دقیق بین "جوان" و "میان‌سال" کجاست؟
    

مجموعه‌های کلاسیک نمی‌توانند این مفاهیم مبهم (Vague) یا نادقیق (Imprecise) را به درستی مدل‌سازی کنند.

### نیاز به مجموعه‌های فازی

نیاز به مجموعه‌های فازی دقیقاً از همین محدودیت مجموعه‌های کلاسیک ناشی می‌شود. ما به سیستمی نیاز داریم که بتواند **درجات عضویت (Degrees of Membership)** را بپذیرد. در دنیای واقعی، عضویت اغلب یک موضوع "بله یا خیر" نیست، بلکه یک موضوع "کم و بیش" است.

- فردی با قد ۱۸۵ سانتی‌متر، **بیشتر** از فردی با قد ۱۷۵ سانتی‌متر عضو مجموعه "بلند قد" است.
    
- دمای ۲۸ درجه، **تا حدی** عضو مجموعه "گرم" است، در حالی که دمای ۳۸ درجه **به طور کامل** عضو این مجموعه است.
    

نظریه مجموعه‌های فازی ابزاری ریاضی برای مدل‌سازی این عدم قطعیت و ابهام فراهم می‌کند.

### ایده اصلی نظریه فازی

ایده اصلی نظریه مجموعه‌های فازی، که در سال ۱۹۶۵ توسط پروفسور **لطفی‌ زاده** معرفی شد، بسیار ساده است:

> به جای اینکه عضویت یک عنصر در یک مجموعه فقط ۰ (عضو نیست) یا ۱ (عضو است) باشد، می‌تواند هر عددی در بازه پیوسته **[0, 1]** باشد.

این عدد، **درجه عضویت** نامیده می‌شود.

- **درجه عضویت ۰:** نشان می‌دهد که عنصر _قطعاً_ عضو مجموعه نیست.
    
- **درجه عضویت ۱:** نشان می‌دهد که عنصر _قطعاً_ عضو مجموعه است.
    
- **درجه عضویت بین ۰ و ۱:** نشان می‌دهد که عنصر _تا حدی_ عضو مجموعه است. هرچه این عدد به ۱ نزدیک‌تر باشد، درجه عضویت بیشتر است.
    

برای مثال، در مجموعه فازی "جوان" (در بازه سنی ۰ تا ۸۰ سال):

- یک فرد ۲۰ ساله ممکن است درجه عضویت ۱.۰ داشته باشد.
    
- یک فرد ۳۰ ساله ممکن است درجه عضویت ۰.۸ داشته باشد.
    
- یک فرد ۴۵ ساله ممکن است درجه عضویت ۰.۳ داشته باشد.
    
- یک فرد ۷۰ ساله ممکن است درجه عضویت ۰.۰ داشته باشد.
    

# فصل ۲: تعاریف و مفاهیم مقدماتی

---

### تابع عضویت (Membership Function)

تابع عضویت، قلب یک مجموعه فازی است. این تابع، درجه عضویت هر عنصر را در آن مجموعه مشخص می‌کند.

اگر `A` یک مجموعه فازی باشد و `X` مجموعه مرجع (Universe of Discourse) ما باشد (مثلاً مجموعه تمام سن‌های ممکن)، تابع عضویت `A` که معمولاً با `μ_A` (بخوانید: مو-ی-اِی) نشان داده می‌شود، تابعی است که هر عنصر `x` از `X` را به یک عدد در بازه `[0, 1]` نگاشت می‌کند.

```
μ_A: X → [0, 1]
```

مقدار `μ_A(x)` درجه‌ای است که `x` به مجموعه `A` تعلق دارد.

**مثال:** فرض کنید `X = {10, 20, 30, 40, 50}` مجموعه سن افراد باشد. می‌خواهیم مجموعه فازی `A` (جوان) را تعریف کنیم. تابع عضویت آن می‌تواند به این صورت باشد:

- `μ_A(10) = 1.0` (۱۰ سالگی قطعاً جوان است)
    
- `μ_A(20) = 1.0` (۲۰ سالگی قطعاً جوان است)
    
- `μ_A(30) = 0.7` (۳۰ سالگی تا حدی جوان است)
    
- `μ_A(40) = 0.2` (۴۰ سالگی کمی جوان است)
    
- `μ_A(50) = 0.0` (۵۰ سالگی اصلاً جوان نیست)
    

### مفاهیم پایه: تکیه‌گاه، هسته (Core)، ارتفاع، نقطه گذر

بر اساس تابع عضویت، چند مفهوم کلیدی تعریف می‌شود:

- **تکیه‌گاه (Support):** مجموعه‌ای از عناصر در `X` که درجه عضویت آن‌ها **بزرگتر از صفر** است. این‌ها عناصری هستند که _حداقل_ کمی عضو مجموعه فازی محسوب می‌شوند.
    
    ```
    Support(A) = { x ∈ X | μ_A(x) > 0 }
    ```
    
    _در مثال بالا: `{10, 20, 30, 40}`_
    
- **هسته (Core):** مجموعه‌ای از عناصر در `X` که درجه عضویت آن‌ها **دقیقاً برابر با ۱** است. این‌ها عناصری هستند که _کاملاً_ عضو مجموعه فازی محسوب می‌شوند.
    
    ```
    Core(A) = { x ∈ X | μ_A(x) = 1 }
    ```
    
    _در مثال بالا: `{10, 20}`_
    
- **ارتفاع (Height):** بزرگترین درجه عضویت در کل مجموعه فازی.
    
    ```
    Height(A) = max(μ_A(x))  (برای تمام x ها در X)
    ```
    
    _در مثال بالا: `1.0`_
    
    - **نکته:** اگر ارتفاع یک مجموعه فازی برابر با ۱ باشد، آن را **نرمال (Normal)** می‌گویند.
        
- **نقطه گذر (Crossover Point):** عنصری که درجه عضویت آن **دقیقاً برابر با ۰.۵** است. این نقطه، مرز میانی ابهام را نشان می‌دهد.
    
    ```
    Crossover(A) = { x ∈ X | μ_A(x) = 0.5 }
    ```
    
    _در مثال بالا، ما نقطه‌ای با عضویت دقیق ۰.۵ نداشتیم، اما اگر `μ_A(35) = 0.5` بود، آنگاه ۳۵ نقطه گذر بود._
    

### نمادگذاری مجموعه‌های فازی

مجموعه‌های فازی را به دو شکل اصلی نمایش می‌دهند:

۱. برای مجموعه‌های گسسته (Discrete):

از نماد سیگما (Σ) یا علامت جمع (+) استفاده می‌شود (توجه: این به معنای جمع ریاضی نیست، بلکه نمادی برای "اجتماع" اعضاست).

```
A = { μ_A(x1)/x1 + μ_A(x2)/x2 + ... }
```

یا

```
A = Σ [ μ_A(xi) / xi ]
```

- **تفسیر:** هر جمله `μ_A(x)/x` به این معناست که عنصر `x` با درجه عضویت `μ_A(x)` در مجموعه `A` قرار دارد. خط کسری `/` نیز به معنای تقسیم نیست، بلکه یک جداکننده نمادین است.
    
- **مثال "جوان" ما:**
    
    ```
    A = { 1.0/10 + 1.0/20 + 0.7/30 + 0.2/40 + 0.0/50 }
    ```
    
    (معمولاً اعضایی که درجه عضویت صفر دارند نوشته نمی‌شوند).
    
    ```
    A = { 1.0/10 + 1.0/20 + 0.7/30 + 0.2/40 }
    ```
    

۲. برای مجموعه‌های پیوسته (Continuous):

از نماد انتگرال (∫) استفاده می‌شود (باز هم به معنای انتگرال‌گیری ریاضی نیست، بلکه نماد اجتماع پیوسته است).

```
A = ∫ [ μ_A(x) / x ]
```

- **مثال:** مجموعه فازی "نزدیک به ۵" روی اعداد حقیقی.
    

### عدد اصلی (Cardinality)

عدد اصلی در مجموعه‌های کلاسیک، تعداد اعضای مجموعه است. در مجموعه‌های فازی، عدد اصلی (که گاهی "توان فازی" یا Scalar Cardinality نامیده می‌شود) برابر است با **جمع تمام درجات عضویت** اعضا.

```
|A| = Σ μ_A(x)   (برای تمام x ها در X)
```

- **تفسیر:** این عدد نشان می‌دهد که "در مجموع چقدر" اعضا در مجموعه حضور دارند.
    
- **مثال "جوان" ما:**
    
    ```
    |A| = 1.0 + 1.0 + 0.7 + 0.2 + 0.0 = 2.9
    ```
    
    این یعنی مجموعه "جوان" ما به اندازه "۲.۹ نفر" عضو دارد.
    

# فصل ۳: عملگرهای مجموعه‌ای استاندارد

---

در این فصل، عملگرهای پایه‌ای مجموعه‌های فازی (متمم، اجتماع، اشتراک) را بررسی می‌کنیم که توسط لطفی‌زاده معرفی شدند و به "عملگرهای استاندارد" معروف‌اند.

### تعاریف پایه: تهی، تام، زیرمجموعه، متمم

- **مجموعه تهی فازی (Empty Set - Ø):** مجموعه‌ای که درجه عضویت تمام عناصر در آن صفر است.
    
    ```
    μ_Ø(x) = 0   (برای تمام x ها)
    ```
    
- **مجموعه تام فازی (Universal Set - X):** مجموعه‌ای که درجه عضویت تمام عناصر در آن یک است. (این همان مجموعه مرجع کلاسیک است).
    
    ```
    μ_X(x) = 1   (برای تمام x ها)
    ```
    
- **زیرمجموعه (Subset - ⊆):** مجموعه `A` زیرمجموعه `B` است اگر و تنها اگر درجه عضویت _هر عنصر_ در `A` کوچکتر یا مساوی درجه عضویت آن در `B` باشد.
    
    ```
    A ⊆ B  <=>  μ_A(x) ≤ μ_B(x)   (برای تمام x ها)
    ```
    
    - **نکته:** اگر `A ⊆ B` باشد، آنگاه `|A| ≤ |B|`.
        
- **متمم (Complement - A'):** متمم مجموعه فازی `A` (بخوانید آ-پریم یا نات-اِی)، مجموعه‌ای است که درجه عضویت هر عنصر در آن برابر است با **یک منهای** درجه عضویت آن در `A`.
    
    ```
    μ_A'(x) = 1 - μ_A(x)
    ```
    
    - مثال: اگر A مجموعه "جوان" باشد، A' مجموعه "غیر جوان" است.
        
        A = { 1.0/20, 0.7/30, 0.2/40 }
        
        A' = { (1-1.0)/20 + (1-0.7)/30 + (1-0.2)/40 }
        
        A' = { 0.0/20 + 0.3/30 + 0.8/40 }
        

### اجتماع و اشتراک (با مثال)

عملگرهای استاندارد برای اجتماع و اشتراک، به ترتیب توابع **ماکزیمم (max)** و **مینیمم (min)** هستند.

- **اجتماع (Union - ∪):** اجتماع دو مجموعه فازی `A` و `B` (یعنی `A` _یا_ `B`)، مجموعه‌ای است که درجه عضویت هر عنصر در آن برابر است با **بیشترین** (max) درجه عضویت آن عنصر در دو مجموعه.
    
    ```
    μ_{A ∪ B}(x) = max( μ_A(x), μ_B(x) )
    ```
    
- **اشتراک (Intersection - ∩):** اشتراک دو مجموعه فازی `A` و `B` (یعنی `A` _و_ `B`)، مجموعه‌ای است که درجه عضویت هر عنصر در آن برابر است با **کمترین** (min) درجه عضویت آن عنصر در دو مجموعه.
    
    ```
    μ_{A ∩ B}(x) = min( μ_A(x), μ_B(x) )
    ```
    

---

**مثال جامع:**

فرض کنید مجموعه مرجع ما X = {1, 2, 3, 4, 5} باشد.

دو مجموعه فازی زیر را در نظر بگیرید:

- `A` (مجموعه "اعداد کوچک"):
    
    ```
    A = { 1.0/1 + 0.8/2 + 0.5/3 + 0.2/4 + 0.0/5 }
    ```
    
- `B` (مجموعه "اعداد حدود ۳"):
    
    ```
    B = { 0.1/1 + 0.5/2 + 1.0/3 + 0.5/4 + 0.1/5 }
    ```
    

حالا عملگرها را اعمال می‌کنیم:

۱. متمم A (یعنی "اعداد غیر کوچک"):

A' = { (1-1.0)/1 + (1-0.8)/2 + (1-0.5)/3 + (1-0.2)/4 + (1-0.0)/5 } A' = { 0.0/1 + 0.2/2 + 0.5/3 + 0.8/4 + 1.0/5 }

۲. اجتماع A و B (یعنی "کوچک" یا "حدود ۳"):

A ∪ B = { max(1.0, 0.1)/1 + max(0.8, 0.5)/2 + max(0.5, 1.0)/3 + max(0.2, 0.5)/4 + max(0.0, 0.1)/5 } A ∪ B = { 1.0/1 + 0.8/2 + 1.0/3 + 0.5/4 + 0.1/5 }

* تفسیر: عنصر 2 در A درجه عضویت 0.8 و در B درجه 0.5 داشت. چون 0.8 (بیشینه) است، در مجموعه اجتماع با 0.8 ظاهر می‌شود.

۳. اشتراک A و B (یعنی "کوچک" و "حدود ۳"):

A ∩ B = { min(1.0, 0.1)/1 + min(0.8, 0.5)/2 + min(0.5, 1.0)/3 + min(0.2, 0.5)/4 + min(0.0, 0.1)/5 } A ∩ B = { 0.1/1 + 0.5/2 + 0.5/3 + 0.2/4 + 0.0/5 }

* تفسیر: عنصر 3 در A درجه عضویت 0.5 و در B درجه 1.0 داشت. چون 0.5 (کمینه) است، در مجموعه اشتراک با 0.5 ظاهر می‌شود.

# فصل ۴: ویژگی‌های عملگرها و ساختار مجموعه‌های فازی

---

### قوانین استاندارد (Idempotency, Commutativity, Associativity, Distributivity, De Morgan)

عملگرهای استاندارد فازی (min و max) بسیاری از قوانین مجموعه‌های کلاسیک را حفظ می‌کنند:

- **خود توانی (Idempotency):**
    
    ```
    A ∪ A = A
    A ∩ A = A
    ```
    
    _(چون `max(μ, μ) = μ` و `min(μ, μ) = μ`)_
    
- **جابجایی (Commutativity):**
    
    ```
    A ∪ B = B ∪ A
    A ∩ B = B ∩ A
    ```
    
- **شرکت‌پذیری (Associativity):**
    
    ```
    (A ∪ B) ∪ C = A ∪ (B ∪ C)
    (A ∩ B) ∩ C = A ∩ (B ∩ C)
    ```
    
- **توزیع‌پذیری (Distributivity):**
    
    ```
    A ∩ (B ∪ C) = (A ∩ B) ∪ (A ∩ C)
    A ∪ (B ∩ C) = (A ∪ B) ∩ (A ∪ C)
    ```
    
- **قوانین دمورگان (De Morgan's Laws):**
    
    ```
    (A ∪ B)' = A' ∩ B'
    (A ∩ B)' = A' ∪ B'
    ```
    

### تفاوت‌ها با مجموعه‌های کلاسیک

مهم‌ترین تفاوت مجموعه‌های فازی با مجموعه‌های کلاسیک، نقض دو قانون اساسی منطق ارسطویی است:

۱. قانون طرد شق ثالث (Law of Excluded Middle):

* در مجموعه‌های کلاسیک: A ∪ A' = X (هر عنصری یا در A است یا در متمم A).

* در مجموعه‌های فازی: A ∪ A' ≠ X (لزوماً)

* چرا؟

μ_{A ∪ A'}(x) = max( μ_A(x), 1 - μ_A(x) )

اگر μ_A(x) = 0.6 باشد، آنگاه μ_A'(x) = 0.4.

max(0.6, 0.4) = 0.6. این مقدار 1 (مجموعه تام) نیست.

۲. قانون عدم تناقض (Law of Non-Contradiction):

* در مجموعه‌های کلاسیک: A ∩ A' = Ø (هیچ عنصری نمی‌تواند همزمان در A و متمم A باشد).

* در مجموعه‌های فازی: A ∩ A' ≠ Ø (لزوماً)

* چرا؟

μ_{A ∩ A'}(x) = min( μ_A(x), 1 - μ_A(x) )

اگر μ_A(x) = 0.6 باشد، آنگاه μ_A'(x) = 0.4.

min(0.6, 0.4) = 0.4. این مقدار 0 (مجموعه تهی) نیست.

- **تفسیر:** این ویژگی دقیقاً همان چیزی است که به مجموعه‌های فازی قدرت می‌دهد. یک عنصر می‌تواند _تا حدی_ عضو `A` باشد و _همزمان تا حدی_ عضو متمم `A` (یعنی `A'`) باشد. این همان مدل‌سازی ابهام است.
    

### افراز فازی (Fuzzy Partition)

در مجموعه‌های کلاسیک، افراز (Partition) به معنای تقسیم مجموعه مرجع به زیرمجموعه‌هایی است که **دو به دو جدا (Disjoint)** باشند و **اجتماع** آن‌ها کل مجموعه را بسازد.

در مجموعه‌های فازی، مفهوم افراز متفاوت است. یک افراز فازی مجموعه‌ای از مجموعه‌های فازی `{A1, A2, ..., An}` است که یک شرط کلیدی را برآورده می‌کنند (تعاریف مختلفی وجود دارد، اما رایج‌ترین آن‌ها):

**شرط افراز فازی:** مجموع درجات عضویت هر عنصر در _تمام_ مجموعه‌های افراز باید برابر با ۱ باشد.

```
Σ μ_Ai(x) = 1   (برای هر x و برای i از 1 تا n)
```

- **تفسیر:** این بدان معناست که هر عنصر `x`، عضویت کامل خود (که ۱ است) را بین مجموعه‌های مختلف افراز "تقسیم" می‌کند. این مجموعه‌ها لزوماً جدا از هم نیستند (اشتراک آن‌ها می‌تواند ناتهی باشد)، اما در مجموع، تمام "انرژی عضویت" را پوشش می‌دهند.
    

### ساختار جبری

- مجموعه‌های کلاسیک تحت عملگرهای اجتماع، اشتراک و متمم، ساختاری به نام **جبر بول (Boolean Algebra)** را تشکیل می‌دهند.
    
- مجموعه‌های فازی تحت عملگرهای استاندارد (max, min, 1-μ)، به دلیل نقض قوانین متمم (طرد شق ثالث و عدم تناقض)، دیگر جبر بول _نیستند_.
    
- این ساختار، یک **شبکه توزیعی کراندار (Bounded Distributive Lattice)** نامیده می‌شود.
    

# فصل ۵: سایر عملگرهای مجموعه‌ای

---

عملگرهای `min` و `max` تنها راه تعریف اشتراک و اجتماع نیستند. خانواده‌های دیگری از عملگرها به نام **T-norms** (برای اشتراک) و **S-norms** (برای اجتماع) وجود دارند که انعطاف‌پذیری بیشتری ایجاد می‌کنند.

### جمع جبری، ضرب جبری، جمع کراندار، تفاضل کراندار

در اینجا چند عملگر رایج دیگر معرفی می‌شوند:

- ضرب جبری (Algebraic Product) - (یک T-norm):
    
    این عملگر به عنوان جایگزینی برای min (اشتراک) استفاده می‌شود.
    
    ```
    μ_{A * B}(x) = μ_A(x) * μ_B(x)
    ```
    
    _(ضرب معمولی درجات عضویت)_
    
- جمع جبری (Algebraic Sum) - (یک S-norm):
    
    این عملگر به عنوان جایگزینی برای max (اجتماع) استفاده می‌شود.
    
    ```
    μ_{A + B}(x) = μ_A(x) + μ_B(x) - (μ_A(x) * μ_B(x))
    ```
    
    _(این فرمول تضمین می‌کند که حاصل همیشه در بازه [0, 1] باقی بماند)_
    
- جمع کراندار (Bounded Sum):
    
    یک S-norm دیگر که قوی‌تر از max عمل می‌کند.
    
    ```
    μ_{A ⊕ B}(x) = min(1, μ_A(x) + μ_B(x))
    ```
    
- تفاضل کراندار (Bounded Difference):
    
    جایگزینی برای تفاضل مجموعه‌ها.
    
    ```
    μ_{A ⊖ B}(x) = max(0, μ_A(x) + μ_B(x) - 1)
    ```
    
    _(توجه: این با `A ∩ B'` متفاوت است)_
    

### ضرب اسکالر، توان

می‌توان عملیات ریاضی را مستقیماً روی درجات عضویت اعمال کرد:

- ضرب اسکالر (Scalar Product):
    
    ضرب یک مجموعه فازی A در یک عدد اسکالر s (که 0 ≤ s ≤ 1).
    
    ```
    μ_{sA}(x) = s * μ_A(x)
    ```
    
    _(این عمل باعث "کاهش" ارتفاع مجموعه می‌شود)_
    
- توان (Power):
    
    به توان رساندن درجات عضویت مجموعه A.
    
    ```
    μ_{A^k}(x) = ( μ_A(x) )^k
    ```
    

### عملگرهای تمرکز (CON) و اتساع (DIL)

توان‌های خاص (k=2 و k=0.5) نام‌های ویژه‌ای دارند و به عنوان "تعدیل‌گرهای زبانی (Linguistic Hedges)" استفاده می‌شوند:

- عملگر تمرکز (Concentration - CON):
    
    این عملگر معمولاً با k=2 تعریف می‌شود و معادل زبانی کلمه "خیلی (Very)" است.
    
    ```
    μ_CON(A)(x) = μ_{A^2}(x) = ( μ_A(x) )^2
    ```
    
    - **تفسیر:** چون `μ` عددی بین ۰ و ۱ است، به توان ۲ رساندن، آن را _کوچکتر_ می‌کند (مگر اینکه ۰ یا ۱ باشد). این کار باعث "باریک‌تر" شدن تابع عضویت و "شدیدتر" شدن مفهوم می‌شود.
        
    - مثال: μ_"جوان"(30) = 0.7
        
        μ_"خیلی جوان"(30) = (0.7)^2 = 0.49 (درجه عضویت کاهش یافت)
        
- عملگر اتساع (Dilation - DIL):
    
    این عملگر معمولاً با k=0.5 (رادیکال) تعریف می‌شود و معادل زبانی کلمه "تا حدی (Somewhat)" یا "تقریباً" است.
    
    ```
    μ_DIL(A)(x) = μ_{A^0.5}(x) = sqrt( μ_A(x) )
    ```
    
    - **تفسیر:** جذر گرفتن از عددی بین ۰ و ۱، آن را _بزرگتر_ می‌کند. این کار باعث "پهن‌تر" شدن تابع عضویت و "ملایم‌تر" شدن مفهوم می‌شود.
        
    - مثال: μ_"جوان"(30) = 0.7
        
        μ_"تا حدی جوان"(30) = sqrt(0.7) ≈ 0.83 (درجه عضویت افزایش یافت)
        

### ضرب دکارتی

ضرب دکارتی `A × B`، یک رابطه فازی (Fuzzy Relation) بین دو مجموعه `X` و `Y` ایجاد می‌کند. تابع عضویت آن روی زوج مرتب `(x, y)` تعریف می‌شود:

```
μ_{A × B}(x, y) = min( μ_A(x), μ_B(y) )
```

_(از عملگر `min` استاندارد استفاده می‌شود، اگرچه می‌توان از T-norm های دیگر نیز استفاده کرد)_

# فصل ۶: α-برش‌ها و تحدب

---

α-برش (آلفا-کات) یکی از مهم‌ترین مفاهیم در نظریه فازی است، زیرا **پلی بین مجموعه‌های فازی و مجموعه‌های کلاسیک (Crisp)** ایجاد می‌کند.

### تعریف α-برش و α-برش قوی

یک α-برش، یک مجموعه **کلاسیک (Crisp)** است که با تعیین یک "آستانه عضویت" به نام `α` (آلفا) از دل مجموعه فازی بیرون کشیده می‌شود. `α` عددی بین ۰ و ۱ است.

- α-برش (α-cut):
    
    مجموعه A_α (بخوانید اِی-آلفا)، شامل تمام عناصری از X است که درجه عضویت آن‌ها در A بزرگتر یا مساوی α باشد.
    
    ```
    A_α = { x ∈ X | μ_A(x) ≥ α }
    ```
    
- α-برش قوی (Strong α-cut):
    
    مجموعه A_{α+}، شامل تمام عناصری از X است که درجه عضویت آن‌ها در A اکیداً بزرگتر از α باشد.
    
    ```
    A_{α+} = { x ∈ X | μ_A(x) > α }
    ```
    

### مثال‌ها و ویژگی‌ها

**مثال:** مجموعه فازی `A` را در نظر بگیرید:

```
A = { 1.0/a + 0.9/b + 0.7/c + 0.5/d + 0.2/e }
```

بیایید α-برش‌های مختلف آن را محاسبه کنیم (اینها مجموعه‌های کلاسیک هستند):

- α = 1.0
    
    A_1.0 = { x | μ_A(x) ≥ 1.0 } = { a }
    
- α = 0.8
    
    A_0.8 = { x | μ_A(x) ≥ 0.8 } = { a, b }
    
- α = 0.5
    
    A_0.5 = { x | μ_A(x) ≥ 0.5 } = { a, b, c, d }
    
- α = 0.0
    
    A_0.0 = { x | μ_A(x) ≥ 0.0 } = { a, b, c, d, e } (این همیشه کل مجموعه مرجع است)
    

حالا یک **برش قوی** برای `α = 0.5` محاسبه می‌کنیم:

- `A_{0.5+} = { x | μ_A(x) > 0.5 } = { a, b, c }` (عنصر `d` حذف شد چون عضویتش دقیقاً ۰.۵ بود نه بزرگتر).
    

ویژگی کلیدی (آشیانه‌ای):

اگر α_1 < α_2 باشد، آنگاه A_{α_2} ⊆ A_{α_1}.

- **تفسیر:** هرچه آستانه `α` را بالاتر ببریم، مجموعه α-برش حاصل، کوچکتر یا مساوی مجموعه قبلی می‌شود. (در مثال بالا `A_0.8 ⊆ A_0.5`).
    

### قضیه تجزیه (Decomposition Theorem)

این قضیه بنیادی‌ترین ارتباط بین مجموعه‌های فازی و کلاسیک را نشان می‌دهد. قضیه تجزیه می‌گوید:

> **هر مجموعه فازی را می‌توان به صورت اجتماع (بازسازی) تمام α-برش‌هایش (که مجموعه‌های کلاسیک هستند) بازنویسی کرد، به شرطی که هر α-برش در سطح `α` خود "وزن" داده شود.**

فرمول دقیق آن (که از `supremum` استفاده می‌کند) کمی پیچیده است، اما مفهوم ساده‌ای دارد:

```
μ_A(x) = sup { α * 1_{A_α}(x) | α ∈ (0, 1] }
```

(که در آن `1_{A_α}(x)` تابع مشخصه کلاسیک است: اگر `x` در `A_α` باشد ۱ وگرنه ۰ است)

- **تفسیر ساده:** درجه عضویت عنصر `x` در مجموعه فازی `A`، برابر است با **بزرگترین `α`**یی که `x` _هنوز_ در α-برش `A_α` حضور دارد.
    
- مثال: در مجموعه قبلی، μ_A(c) = 0.7.
    
    c در A_0.5 هست.
    
    c در A_0.7 هست.
    
    c در A_0.8 نیست.
    
    بنابراین بزرگترین α که c در آن حضور دارد 0.7 است، که همان درجه عضویت c است.
    

### کاربرد در عدد اصلی فازی

قضیه تجزیه به ما اجازه می‌دهد که عدد اصلی فازی (که قبلاً `Σ μ_A(x)` تعریف شد) را به روش دیگری، بر اساس انتگرال‌گیری روی α-برش‌های کلاسیک محاسبه کنیم (این روش به "انتگرال چوکوت" معروف است).

### تحدب و کراندار بودن

- کراندار بودن (Bounded):
    
    یک مجموعه فازی "کراندار" است اگر تکیه‌گاه (Support) آن یک مجموعه کراندار (در مجموعه مرجع) باشد.
    
- تحدب (Convexity):
    
    این مفهوم در مجموعه‌های فازی بسیار مهم است. یک مجموعه فازی A محدب (Convex) نامیده می‌شود اگر و تنها اگر تمام α-برش‌های آن (A_α) مجموعه‌های محدب کلاسیک باشند.
    
    - **تفسیر در مجموعه‌های کلاسیک:** یک مجموعه (مثلاً روی خط اعداد) محدب است اگر برای هر دو نقطه `x1` و `x2` در مجموعه، تمام نقاط _بین_ آن‌ها نیز در مجموعه باشند. (یعنی مجموعه "گسستگی" یا "چاله" در وسط نداشته باشد).
        
    - **تفسیر در مجموعه‌های فازی:** اگر یک مجموعه فازی (مثلاً روی خط اعداد) محدب باشد، تابع عضویت آن "تک-قله‌ای" (unimodal) خواهد بود. یعنی تابع عضویت از ۰ شروع شده، به ۱ (یا ارتفاع خود) می‌رسد و سپس دوباره به ۰ کاهش می‌یابد، **بدون اینکه در این بین "دره" یا "کاهش و افزایش مجدد" داشته باشد.**
        
    - **تعریف رسمی ریاضی:**
        
        ```
        μ_A( λ*x1 + (1-λ)*x2 )  ≥  min( μ_A(x1), μ_A(x2) )
        ```
        
        (برای هر `x1`, `x2` در `X` و هر `λ` بین ۰ و ۱)