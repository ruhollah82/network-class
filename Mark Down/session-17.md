# درسنامه شبکه‌های کامپیوتری - جلسه هفدهم (پایانی)
**موضوعات اصلی:** نکات پروژه پایانی، لایه پیوند داده (Data Link Layer)، روش‌های کشف خطا، انواع روش‌های آدرس‌دهی و ارسال در شبکه.

---

## بخش اول: نکات مربوط به پروژه و ارائه پایانی

پیش از شروع درس، استاد توضیحاتی درباره نحوه ارائه پروژه (معمولاً شبیه‌سازی با Packet Tracer) دادند:

1.  **هدف از پروژه:** هدف این نیست که شما تمام جزئیات نرم‌افزار شبیه‌ساز را یاد بگیرید، بلکه باید روی **سناریوی خودتان** اشراف داشته باشید.
2.  **تسلط بر سناریو:**
    *   باید بدانید چرا این مدل روتر یا سوئیچ را انتخاب کردید.
    *   باید بتوانید توضیح دهید که سناریوی شما چه کاری انجام می‌دهد (مثلاً ارسال ایمیل، اتصال طبقات مختلف یک ساختمان، پروتکل HTTP و ...).
    *   پرزنتیشن (ارائه) نمره دارد و باید بتوانید به سوالات استاد درباره تنظیمات پاسخ دهید.
3.  **نمره دهی:**
    *   نمره بر اساس "میانگین" (Average) داده می‌شود (معمولاً همه نمره قبولی و خوب را می‌گیرند).
    *   تفاوت نمرات (مثلاً نیم نمره یا یک نمره) بستگی به **خلاقیت، ایده بهتر و ارائه قوی‌تر** دارد.
    *   اگر پروژه کپی باشد یا اصلاً ارائه ندهید، نمره کسر خواهد شد.
4.  **پیشنهاد برای سناریو:**
    *   می‌توانید از پروتکل‌های مختلف مثل DHCP, HTTP, Mail و Routing در Packet Tracer استفاده کنید.
    *   مثال دانشجو: یک ساختمان سه طبقه (رستوران) که طبقات مختلف (لوجستیک، مالی، IT) با روتر به هم وصل شده‌اند.

---

## بخش دوم: لایه پیوند داده (Data Link Layer)

این لایه، دومین لایه در مدل OSI است. استاد تاکید کردند که **لایه فیزیکی (Physical Layer)** به دلیل مباحث سخت‌افزاری و سیم‌کشی در این درس نادیده گرفته می‌شود.

### ۱. تعریف کلی
لایه Data Link یک مفهوم انتزاعی است که وظایف زیر را بر عهده دارد:
*   **پنهان‌سازی جزئیات سخت‌افزاری:** به عنوان واسطه‌ای برای ارتباط عمل می‌کند.
*   **تبدیل داده:** مسئول تبدیل جریان داده (Data Stream) به بیت‌های سیگنال و ارسال آن‌ها روی سخت‌افزار است.

### ۲. مفاهیم شش‌گانه در لایه پیوند داده
در این لایه، ۶ مفهوم کلیدی تعریف می‌شود:

#### الف) قاب‌بندی (Framing)
*   این لایه بسته داده (Packet) را از لایه بالاتر (لایه شبکه/Network) می‌گیرد و آن را در قالب‌هایی به نام **قاب (Frame)** کپسوله (Encapsulate) می‌کند.
*   **نکته:** داده‌ها در این لایه به اسم "فریم" شناخته می‌شوند و سپس به صورت بیت‌به‌بیت روی سخت‌افزار ارسال می‌شوند.

#### ب) آدرس‌دهی (Addressing)
*   این لایه سازوکار آدرس‌دهی سخت‌افزاری را ارائه می‌کند (همان **آدرس MAC**).
*   این آدرس معمولاً در زمان ساخت قطعه، روی سخت‌افزار کدگذاری می‌شود.

#### ج) همگام‌سازی (Synchronization)
*   زمانی که فریم‌ها ارسال می‌شوند، هم فرستنده (Sender) و هم گیرنده (Receiver) باید با هم **همگام (Sync)** شوند تا انتقال به درستی صورت گیرد.

#### د) کنترل خطا (Error Control)
*   سیگنال‌ها ممکن است در حین انتقال دچار مشکل شوند (مثلاً بیت‌ها تغییر کنند).
*   در این لایه خطاها شناسایی، گزارش‌گیری و داده واقعی بازیابی می‌شود. (جزئیات در بخش سوم).

#### هـ) کنترل جریان (Flow Control)
*   این لایه تضمین می‌کند که داده‌ها با سرعت یکسان مبادله شوند تا گیرنده سرریز نشود.
*   **مثال:** استفاده از مکانیزم "پنجره لغزان" (Sliding Window) که قبلاً اشاره شده بود.

#### و) دسترسی چندگانه (Multi-Access)
*   هنگام انتقال داده بر روی یک لینک مشترک، احتمال "تداخل" یا دسترسی همزمان وجود دارد.
*   این لایه سازوکاری ارائه می‌دهد که ظرفیت دسترسی به لینک بالا برود (مثل مکانیزم CSMA).

---

## بخش سوم: انواع خطا و روش‌های تشخیص خطا

مهم‌ترین بحث این جلسه، نحوه برخورد با خطا در لایه Data Link است.

### ۱. انواع خطا در شبکه
1.  **خطای تک‌بیتی (Single-bit Error):** در یک توالی از بیت‌ها، تنها **یک بیت** تغییر کرده است (مثلاً 0 شده 1).
2.  **خطای چندبیتی (Multi-bit Error):** بیش از یک بیت تغییر کرده است.
3.  **خطای رگباری/متوالی (Burst Error):** چند بیت **پشت سر هم** دچار تغییر شده‌اند (مثلاً بیت‌های اول تا سوم همگی خراب شده‌اند).

### ۲. تکنیک‌های تشخیص خطا (Error Detection)

#### روش اول: Simple Parity Check (توازن ساده)
*   **مکانیزم:** یک بیت اضافه به انتهای داده اضافه می‌شود تا تعداد "یک"ها را زوج (یا فرد) کند.
    *   اگر تعداد 1ها فرد باشد -> بیت مازاد می‌شود 1 (تا زوج شود).
    *   اگر تعداد 1ها زوج باشد -> بیت مازاد می‌شود 0.
*   **نحوه چک کردن:** در مقصد دوباره تعداد 1ها شمرده می‌شود؛ اگر زوج بود، داده سالم است.
*   **ایراد (باگ):** اگر **دو بیت** همزمان تغییر کنند (مثلاً دو تا 1 بشوند 0)، تعداد 1ها همچنان زوج می‌ماند و خطا تشخیص داده نمی‌شود. این روش فقط برای خطای تک‌بیتی خوب است.

#### روش دوم: Two-Dimensional Parity Check (توازن دو بعدی)
*   **مکانیزم:** داده‌ها (مثلاً بسته‌های 8 بیتی) را تکه‌تکه کرده و زیر هم می‌نویسند (مثل ماتریس).
*   بیت Parity هم برای هر **ردیف** و هم برای هر **ستون** محاسبه می‌شود.
*   این روش مشکل روش قبلی را تا حد زیادی حل می‌کند اما سربار (Overhead) بیشتری دارد (اضافه شدن یک ستون و یک ردیف داده اضافی).

#### روش سوم: Checksum (مجموع کنترلی)
این روش در دو مرحله انجام می‌شود:
1.  **در فرستنده:**
    *   داده به $K$ قسمت $N$ بیتی تقسیم می‌شود.
    *   تمام قسمت‌ها با استفاده از **مکمل یک** (One's Complement) با هم جمع می‌شوند.
    *   حاصل جمع (که به آن Checksum Field می‌گویند) همراه داده ارسال می‌شود.
2.  **در گیرنده:**
    *   داده دریافتی دوباره تقسیم و با هم جمع می‌شود (همراه با Checksum دریافتی).
    *   اگر مکملِ حاصل‌جمع نهایی **صفر** شود، داده قبول است (سالم است). در غیر این صورت رد می‌شود.

#### روش چهارم: CRC (Cyclic Redundancy Check)
*   **تعریف:** روشی مبتنی بر **تقسیم باینری**.
*   **مکانیزم:**
    *   **فرستنده:** یک "مقسم" (Divisor) قراردادی وجود دارد. داده بر این عدد تقسیم می‌شود. **باقی‌مانده** تقسیم به انتهای داده اضافه و ارسال می‌شود.
    *   **گیرنده:** داده دریافتی را بر همان مقسم تقسیم می‌کند.
    *   اگر **باقی‌مانده صفر شود**، داده صحیح است.
*   *نکته:* این روش دقیق‌ترین روش بین موارد ذکر شده است و در شبکه بسیار کاربرد دارد. (نیازی به حفظ کردن الگوریتم ریاضی آن برای امتحان نیست، فقط تعریف را بدانید).

---

## بخش چهارم: انواع روش‌های ارسال و آدرس‌دهی (Transmission Modes)


| نوع ارتباط | نام انگلیسی | تعریف و توضیحات استاد | مثال کاربردی |
| :--- | :--- | :--- | :--- |
| **تک‌پخشی** | **Unicast** | ارتباط **یک به یک**. فرستنده مشخص است و گیرنده هم مشخص. | ارسال ایمیل، وب‌گردی (Browsing). |
| **Anycast** | **Anycast** | ارتباط یک به "یکی از چند نفر" (معمولاً نزدیک‌ترین). مثل Unicast است اما گیرنده الزماً یک فرد خاص از پیش تعیین شده نیست، بلکه یک گروه در دسترس است. | سرویس‌های DNS (استاد خیلی وارد جزئیات فنی نشدند). |
| **چندپخشی** | **Multicast** | ارتباط **یک به چند** (یک گروه خاص). داده ارسال می‌شود و فقط گروهی که عضو هستند دریافت می‌کنند. بهینه‌تر از برودکست است. | کنفرانس ویدیویی، استریمینگ ویدیو. |
| **همه‌پخشی** | **Broadcast** | ارتباط **یک به همه**. داده برای تمام اعضای شبکه ارسال می‌شود. ترافیک زیادی ایجاد می‌کند. | پروتکل DHCP (درخواست IP)، پیام‌های اضطراری. |
| **Geocast** | **Geocast** | زیرمجموعه Multicast است که بر اساس **موقعیت جغرافیایی** انجام می‌شود (مثلاً ارسال داده فقط برای قاره آسیا). | سرویس‌های مبتنی بر مکان جغرافیایی. |

### مثال کلاسی استاد برای درک بهتر:
*   **Unicast:** استاد مستقیماً از "آقای X" سوال می‌پرسد (فقط او جواب می‌دهد).
*   **Multicast:** استاد به "ردیف اول" می‌گوید تمرین حل کنند (فقط یک گروه خاص).
*   **Broadcast:** استاد با صدای بلند درس می‌دهد و "همه کلاس" می‌شنوند.
