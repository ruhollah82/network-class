# لایه انتقال (Transport Layer)

**مبحث: قابلیت اطمینان، کنترل جریان و کنترل ازدحام**

## ۱. مقدمه و یادآوری
در جلسات گذشته راجع به دو پروتکل اصلی لایه انتقال یعنی **TCP** و **UDP** صحبت شد (هدرها، وظایف، مزایا و معایب). در این جلسه به بررسی سه ویژگی مهم دیگر در این لایه می‌پردازیم:
1.  قابلیت اطمینان (Reliability)
2.  کنترل جریان (Flow Control)
3.  کنترل ازدحام (Congestion Control)

---

## ۲. قابلیت اطمینان (Reliability)
مهم‌ترین نکته در هر ارتباطی (حتی ارتباطات انسانی)، "اعتماد" است. در شبکه نیز ارتباطی ارزشمند است که **Reliability** یا قابلیت اعتماد بالایی داشته باشد.
*   **تعریف:** اطمینان از اینکه بسته‌ای که ارسال می‌کنید با موفقیت و اطمینان کامل به مقصد می‌رسد.
*   **نکته:** سایر ویژگی‌ها مثل سرعت خوب هستند، اما قابلیت اطمینان اولویت دارد. بررسی و پیاده‌سازی این ویژگی خود یک حیطه کاری بزرگ و شامل الگوریتم‌های پیچیده در شبکه است.

---

## ۳. کنترل جریان (Flow Control)
یکی از وظایف مهم که در هدر TCP نیز (با فیلد ۱۶ بیتی Window Size) به آن اشاره شد، کنترل جریان است.

### الف) مفهوم و ضرورت
*   **تعریف:** کنترل جریان داده به فرستنده می‌گوید که چه مقدار داده باید برای گیرنده ارسال شود تا گیرنده بتواند آن را پردازش کند و هیچ داده‌ای در شبکه از دست نرود (Packet Loss نداشته باشیم).
*   **محل کاربرد:** تأخیرها (Delay) معمولاً در صف‌های شبکه (Queue) ایجاد می‌شوند. اگر تنظیمات درست نباشد، داده‌ها در صف مانده و تأخیر ایجاد می‌شود. کنترل جریان با روش‌هایی مثل Stop-and-Wait یا Sliding Window از ایجاد تأخیر و پکت لاس جلوگیری می‌کند.
*   **اهمیت:** نرخ ارسال داده بر روی پارامترهای مهم شبکه مثل **Throughput (توان عملیاتی)**، **Delay (تأخیر)** و **Packet Loss** تأثیر مستقیم دارد.

### ب) روش‌های کنترل جریان

#### ۱. پروتکل ایست و انتظار (Stop-and-Wait)
این ساده‌ترین روش کنترل جریان است.
*   **نحوه عملکرد:**
    1.  فرستنده یک فریم (Frame) ارسال می‌کند.
    2.  فرستنده متوقف می‌شود و منتظر می‌ماند تا گیرنده تأیید (ACK) کند که فریم را دریافت کرده است.
    3.  پس از دریافت پیام تأیید توسط فرستنده، فریم بعدی ارسال می‌شود.
*   **ویژگی‌ها:**
    *   ارتباط تقریباً یک‌طرفه یا نیمه‌دوطرفه است (همزمان نمی‌توانند ارسال کنند).
    *   **مزیت:** سادگی پیاده‌سازی.
    *   **عیب:** زمان زیادی هدر می‌رود (تأخیر زیاد). فرستنده باید برای هر بیت صبر کند تا جواب بیاید. اگر فاصله زیاد باشد، تأخیر مسافتی هم اضافه می‌شود. توان عملیاتی پایین است.

#### ۲. پروتکل پنجره لغزان (Sliding Window) - روش اصلی
این روش در کتاب و پروتکل TCP (فیلد Window) استفاده می‌شود.
*   **تعریف پنجره:** منظور از پنجره، همان حافظه **بافر (Buffer)** است که مثل یک آرایه برای ذخیره فریم‌ها تعریف می‌شود.
*   **نحوه عملکرد:**
    *   دو بافر (پنجره) تعریف می‌شود: یکی سمت فرستنده و یکی سمت گیرنده.
    *   پنجره فرستنده شامل شماره فریم‌هایی است که اجازه دارد ارسال کند.
    *   پنجره گیرنده شامل شماره فریم‌هایی است که باید دریافت کند.
    *   شماره‌گذاری معمولاً از $0$ تا $N-1$ است.
    *   بر خلاف روش قبلی، فرستنده به جای یک فریم، **$N$ فریم** (به اندازه سایز پنجره) را پشت سر هم می‌فرستد.
*   **ویژگی‌ها:**
    *   ارتباط **Full Duplex** (دوطرفه همزمان) است.
    *   **مزیت:** بهره‌وری و Throughput بسیار بالاتر نسبت به Stop-and-Wait.
    *   **نکته طراحی:** سایز بافر بسیار مهم است. اگر سایز بافر خیلی بزرگ باشد (مثلاً ۱۰۰۰) در مقابل سایز کوچک (مثلاً ۱۰)، تأثیر مستقیمی روی Delay و Throughput دارد. طراح شبکه باید بین نرخ ارسال و سایز بافر تعادل (Balance) برقرار کند.

---

## ۴. مدیریت اتصال (Connection Management) در TCP
همانطور که قبلاً گفته شد، TCP اتصال‌گراست. این اتصال باید ایجاد و در نهایت قطع شود.

### الف) ایجاد ارتباط (Three-Way Handshake)
۱.  **Client:** بیت `SYN` (بدون داده) را برای درخواست ارتباط می‌فرستد.
۲.  **Server:** با ارسال `SYN` و `ACK` درخواست را قبول می‌کند.
۳.  **Client:** با ارسال `ACK` (و احتمالاً داده)، ارتباط را رسماً آغاز می‌کند.

### ب) پایان ارتباط (Closing Connection)
ارتباط نباید رها شود و نیازمند پایان‌بندی رسمی است.
۱.  **Client:** بیت `FIN` را برابر ۱ قرار داده و ارسال می‌کند (یعنی "داده‌های من تمام شد").
۲.  **Server:** بیت `FIN` را دریافت کرده، پیام `ACK` را ارسال می‌کند (تأیید دریافت درخواست پایان).
۳.  **Server:** سپس بیت `FIN` خود را نیز ۱ کرده (به همراه `ACK` که ۱ است) ارسال می‌کند تا بگوید داده‌های سمت سرور هم تمام شده است.
۴.  **Client:** بیت `ACK` نهایی را ارسال می‌کند و ارتباط قطع می‌شود.

---

## ۵. کنترل ازدحام (Congestion Control)
*   **تعریف ازدحام:** زمانی رخ می‌دهد که منابع بسیار زیادی (Too many sources) داده‌های بسیار زیادی را با سرعت بالا ارسال می‌کنند و شبکه توانایی مدیریت (Handle) آن را ندارد.
*   **تفاوت با کنترل جریان:** کنترل جریان مربوط به هماهنگی فرستنده و گیرنده است، اما ازدحام مربوط به کل ترافیک شبکه است.
*   **تأثیرات ازدحام:**
    *   افزایش شدید Delay (تأخیر).
    *   افزایش Packet Loss.
    *   کاهش توان عملیاتی (Throughput).

### سناریوهای ازدحام (بسیار مهم)

#### سناریوی اول: بافر نامحدود، بدون باز-ارسال (Ideal)
*   **مشخصات:** ۲ فرستنده، ۲ گیرنده، ۱ روتر.
*   **شرط مهم:** بافر روتر **نامحدود (Infinite)** است و قابلیت **Retransmission (ارسال مجدد)** وجود ندارد.
*   **نمودارها:**
    *   **Delay:** نمودار نمایی (Exponential) است. با نزدیک شدن به نقطه اشباع، تأخیر به بی‌نهایت میل می‌کند.
    *   **Throughput:** نمودار خطی (Linear) است.
*   **نکته:** یک "نقطه اشباع" (Saturation Point) وجود دارد. بعد از این نقطه رفتار شبکه غیرقابل پیش‌بینی است. در این مدل ساده‌ترین و بهترین حالت تروپوت محاسبه می‌شود اما غیرواقعی است.

#### سناریوی دوم: بافر محدود، با باز-ارسال
*   **مشخصات:** همان ۲ فرستنده و گیرنده و ۱ روتر.
*   **تفاوت:** بافر **محدود (Finite)** است و قابلیت **Retransmission** (ارسال مجدد داده‌های گم شده) وجود دارد.
*   **حالت‌های نمودار Throughput:**
    1.  **حالت Goodput ($\lambda_{in} = \lambda_{out}$):** ایده‌آل‌ترین حالت، ورودی مساوی خروجی.
    2.  **حالت $\lambda_{in} > \lambda_{out}$:** به دلیل وجود قابلیت ارسال مجدد، شیب نمودار تغییر می‌کند اما همچنان صعودی است (داده‌های گم شده دوباره ارسال می‌شوند).
    3.  **حالت $\lambda_{in} \gg \lambda_{out}$:** ورودی بسیار بیشتر از خروجی است، اما فقط تأخیر در صف داریم و داده‌ای گم نمی‌شود (کمتر پیش می‌آید).

#### سناریوی سوم: مدل واقعی‌تر
*   **مشخصات:** ۴ سیستم (که هم فرستنده‌اند هم گیرنده)، ۴ روتر، بافرهای محدود.
*   **ویژگی:** قابلیت باز-ارسال وجود دارد.
*   **نمودار:**
    *   ابتدا رفتار خطی است.
    *   زمانی که شبکه به نقطه اشباع می‌رسد، نمودار **سقوط می‌کند (Drop)**. یعنی توان عملیاتی به شدت کاهش می‌یابد و شبکه قفل می‌کند. این مدل به دنیای واقعی نزدیک‌تر است.

---

## ۶. عدالت در شبکه (Fairness)
علاوه بر Delay و Throughput، پارامتر دیگری به نام **Fairness** وجود دارد.
*   **هدف:** تقسیم عادلانه پهنای باند بین کاربران.
*   **تعریف:** اگر پهنای باند لینک ارتباطی برابر با **$R$** باشد و تعداد کانکشن‌های موجود **$K$** باشد، هدف یک ارتباط عادلانه این است که به هر کاربر به اندازه زیر پهنای باند برسد:
$$ \text{Fair Share} = \frac{R}{K} $$
*   **واقعیت:** در دنیای واقعی به دلایل مختلف (موقعیت فیزیکی، قدرت سخت‌افزاری، نویز محیط، و ...) رسیدن به Fairness کامل دشوار است.
