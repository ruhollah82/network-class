پا
# درسنامه: پروتکل‌های شبکه، سیستم نام دامنه و معماری توزیع فایل

## بخش ۱: ایمیل (پست الکترونیک) و پروتکل‌های آن

### ۱.۱. تعریف و کاربرد ایمیل

ایمیل (پست الکترونیک) یکی از **ساده‌ترین یا شاید معروف‌ترین راه‌ها** برای برقراری ارتباط اینترنتی است. هدف از ایمیل **انتقال اطلاعات و ارتباط** است. کاربران با استفاده از ایمیل می‌توانند مجموعه‌ای از داده‌ها را یا به صورت نامه یا محتوا، ارسال یا دریافت کنند.

### ۱.۲. اجزای اصلی ایمیل

زمانی که یک ایمیل بین فرستنده و گیرنده ارسال می‌شود، سه جزء اصلی درگیر هستند:

1. **عامل کاربری (User Agent):** این بخش معمولاً منظور همان **نرم‌افزارهایی** است که شما به عنوان رابط کاربری (Interface) با ایمیل دارید. نرم‌افزارهای معروفی چون **جیمیل، یاهو، آت‌میل، اوت‌لوک و تاندربرد** مثال‌هایی از عامل کاربری هستند. در این بخش اطلاعاتی مانند آدرس فرستنده، آدرس گیرنده، سابجکت (موضوع)، تاریخ و فرمت ایمیل اهمیت دارند.
2. **سرورهای پیام (Message Servers):** این سرورها مکان‌هایی هستند که اتفاقات ارسال و دریافت داده در آن‌ها رخ می‌دهد.
3. **پروتکل‌ها:** برای اینکه ساختار ایمیلی ارسال یا دریافت شود، مجموعه‌ای از قوانین و دستورالعمل‌ها (پروتکل‌ها) لازم است.

### ۱.۳. پروتکل‌های اصلی ایمیل

در دنیا سه پروتکل معروف برای ایمیل استفاده می‌شود:

|نام پروتکل|وظیفه اصلی|مزایا و معایب کلیدی|
|:--|:--|:--|
|**۱. SMTP**|**ارسال ایمیل**.|**مزایا:** ارسال سریع، امنیت بالا (رمزگذاری). **معایب:** محدودیت در اندازه محتوای ایمیل و آسیب‌پذیری (به دلیل سادگی).|
|**۲. POP3**|**دریافت ایمیل** از سرور.|**مزایا:** **ذخیره‌سازی ایمیل روی سیستم کاربر**، کارایی بدون اینترنت (آفلاین)، کاهش بار سرور. **معایب:** **حذف ایمیل از سرور اصلی**، عدم همگام‌سازی بین سیستم‌ها، از دست رفتن ایمیل‌ها در صورت خرابی سیستم.|
|**۳. IMAP**|**دریافت ایمیل**.|**مزایا:** **ذخیره‌سازی ایمیل در سرور**، امکان بک‌آپ‌گیری، **دسترسی همزمان بر روی چندین سیستم** (همگام‌سازی). **معایب:** نیاز به اینترنت مداوم (عدم حالت آفلاین)، کاهش سرعت و عملکرد سرور به دلیل ذخیره همه ایمیل‌ها.|

**توجه:** SMTP (Simple Mail Transfer Protocol) به سرور ایمیل فرمان می‌دهد که چگونه و به کدام سرور باید ارسال شود و در سمت مبدأ (سرور کاربر) قرار دارد. POP3 (Post Office Protocol version 3) پروتکل استاندارد دریافت است که نسخه‌های یک و دو آن موفق نبوده‌اند. IMAP (Internet Message Access Protocol) نیز پروتکلی برای دریافت است.

### ۱.۴. ساختار کلی پیام ایمیل

فرمت عمومی یک پیام ایمیل شامل سه بخش اصلی است:

1. **Header (سربرگ):** شامل آدرس فرستنده (From)، گیرنده (To) و سابجکت (موضوع ایمیل).
2. **Blank Line (خط خالی):** یک خط خالی بعد از هدر.
3. **Body (متن پیام):** همان مسیجی است که ارسال شده و نهایتاً به صفر و یک تبدیل می‌شود.

---

## بخش ۲: سیستم نام دامنه (DNS)

### ۲.۱. مفهوم و ضرورت DNS

- **IP:** در شبکه، هر سیستم یا دیوایس دارای یک **شناسه منحصر به فرد (آی‌پی)** است. سیستم‌ها در شبکه با این آی‌پی شناسایی می‌شوند و ارتباط برقرار می‌کنند.
- **مشکل:** حفظ کردن این اعداد طولانی (آی‌پی) در سطح دنیا برای کاربران سخت است.
- **راه‌حل (DNS):** **سیستم نام دامنه (Domain Name System)** سیستمی است که یک نام (مانند google.com) را به آن عدد (آی‌پی) اطلاق می‌کند تا دسترسی و یادآوری آن آسان‌تر باشد.
- **تعریف:** DNS پروتکلی است برای **ساده کردن فرایند دسترسی به اطلاعات** هر وب‌سایت در اینترنت.

### ۲.۲. ساختار سلسله مراتبی دامنه‌ها

دامنه‌ها ساختاری سلسله مراتبی (Hierarchical) دارند. این ساختار به طور عمومی شامل سه سطح است:

1. **سطح ریشه (Root Level):** بالاترین سطح است که معمولاً به صورت یک نقطه در نظر گرفته می‌شود.
2. **سطح بالا (Top Level Domain - TLD):** شامل حوزه‌ها و دسته‌های اصلی است.
3. **سطح دوم (Second Level):** آدرس‌های نهایی در این سطح قرار می‌گیرند (مثل **yahoo**.com). این سطح ممکن است دارای سطوح زیرمجموعه (ساب لول) نیز باشد.

### ۲.۳. حوزه‌های سطح بالا (TLD)

سطح TLD به هفت حوزه اصلی و تعداد زیادی حوزه کشوری تقسیم می‌شود:

|حوزه TLD|نام انگلیسی|کاربرد|
|:--|:--|:--|
|**com.**|Commercial|مؤسسات اقتصادی و تجاری.|
|**edu.**|Education|مؤسسات آموزشی.|
|**gov.**|Government|دولت (مانند دولت آمریکا و آژانس‌های دولتی آن).|
|**int.**|International|سازمان‌های بین‌المللی (مثل یونسکو).|
|**mil.**|Military|ارتش (مانند ارتش دولت آمریکا).|
|**net.**|Network|شرکت‌های شبکه‌ای و ارائه‌دهندگان خدمات شبکه (Service Providers).|
|**org.**|Organization|سازمان‌های خصوصی و غیرانتفاعی.|

**حوزه‌های کشوری:** علاوه بر این هفت حوزه، بیش از ۱۰۰ یا ۲۰۰ حوزه کشوری نیز در سطح TLD تعریف می‌شوند (مانند **.ir** برای ایران، **.jp** برای ژاپن، **.uk** برای انگلیس).

### ۲.۴. سرور نام محلی (Local Name Server - LNS)

LNS یک سرور محلی است که توسط هر ارائه‌دهنده خدمات اینترنتی (**ISP**) برای شبکه مربوطه تعریف می‌شود. زمانی که میزبان (Host) درخواستی ارسال می‌کند، ابتدا به LNS می‌رود. این سرور درخواست را ذخیره می‌کند تا در ارتباطات بعدی سریع‌تر بازیابی شود.

### ۲.۵. انواع کوئری (درخواست) در DNS

یک جستجوی معمولی در DNS شامل دو مدل اصلی کوئری است که باید شکل، شماره‌ها و فلش‌های آن‌ها در نظر گرفته شود:

1. **کوئری تکراری (Iterative Query):**
    
    - در این مدل، سرویس‌گیرنده DNS درخواست خود را به سرور ریشه ارسال کرده و پاسخ را دریافت می‌کند.
    - سپس سرویس‌گیرنده **دوباره** برای سطح پایین‌تر (مانند TLD) درخواست ارسال کرده و جواب دوم را دریافت می‌کند.
    - این روند ادامه می‌یابد (به صورت تکراری) تا آدرس نهایی بازیابی شود.
2. **کوئری بازگشتی (Recursive Query):**
    
    - در این مدل، سرویس‌گیرنده درخواست خود را به سرور ریشه می‌فرستد.
    - **سرور اصلی (روت) مسئول پیدا کردن آدرس است** و با ارتباط با سطوح بعدی، آدرس را پیدا می‌کند.
    - در نهایت، جواب از سطح آخر به سطح ریشه باز می‌گردد و به کاربر داده می‌شود.

---

## بخش ۳: معماری همتا به همتا (P2P) و توزیع فایل

### ۳.۱. ماهیت معماری P2P

معماری همتا به همتا (Peer-to-Peer) بر خلاف مدل کلاینت-سرور، **قضیه سرور همیشه آنلاین** را ندارد.

- **وظایف:** در معماری P2P، همه دیوایس‌ها می‌توانند در لحظه **هم سرور (سرویس‌دهنده) و هم کلاینت (سرویس‌گیرنده)** باشند.
- **توزیع‌شدگی (Distribution):** اگر سیستمی در شبکه P2P خراب شود، سیستم‌های دیگر جایگزین شده و به کمک کردن ادامه می‌دهند، لذا ارتباط قطع نمی‌شود.
- **کاربرد:** P2P یک معماری مناسب برای **توزیع فایل (File Sharing)** است.

### ۳.۲. مقایسه زمان توزیع فایل

زمان توزیع فایل (Distribution Time) بسته به مدل شبکه (کلاینت-سرور یا P2P) متفاوت محاسبه می‌شود.

**پارامترهای مورد نیاز:**

- $F$: سایز فایل (حجم فایل).
- $N$: تعداد کاربران (یوزرها).
- $U_s$: نرخ آپلود سرور.
- $D_i$: نرخ دانلود کاربر $i$.
- $U_i$: نرخ آپلود کاربر $i$.

#### ۱. زمان توزیع در مدل کلاینت-سرور ($D_{CS}$)

در این مدل، سرور داده را آپلود کرده و سیستم‌ها دانلود یا آپلود می‌کنند. زمان توزیع بر اساس ماکزیمم دو بخش محاسبه می‌شود:

$$\mathbf{D_{CS} = \text{Maximum} \left{ \frac{N \times F}{U_s}, \frac{F}{\text{Minimum}(D_i)} \right}}$$

- **تحلیل:** این فرمول دارای رابطه **خطی (Linear)** با $N$ (تعداد کاربران) و $F$ (سایز فایل) است. هرچه تعداد کاربران ($N$) بیشتر باشد، زمان توزیع فایل افزایش پیدا می‌کند.

#### ۲. زمان توزیع در مدل همتا به همتا ($D_{P2P}$)

در مدل P2P، سرور به تنهایی کار نمی‌کند و نرخ آپلود سرور به نرخ آپلود کاربران اضافه می‌شود. زمان توزیع بر اساس ماکزیمم سه بخش محاسبه می‌شود:

$$\mathbf{D_{P2P} = \text{Maximum} \left{ \frac{F}{U_s}, \frac{F}{\text{Minimum}(D_i)}, \frac{F}{U_s + \sum_{i=1}^{N} U_i} \right}}$$

- **تحلیل و مقایسه:** مدل P2P **بهتر** است. در این مدل، پس از یک مدتی، حتی با افزایش تعداد کاربران، زمان توزیع ثابت باقی می‌ماند و افزایش نمی‌یابد (شکل خط افقی).

---

**تشبیه برای درک عملکرد P2P:** اگر بخواهیم تفاوت مدل کلاینت-سرور و P2P را در توزیع فایل درک کنیم، می‌توان آن را به توزیع برگه‌های امتحانی در یک کلاس بزرگ تشبیه کرد: در مدل **کلاینت-سرور**، فقط استاد (سرور) می‌تواند برگه‌ها را توزیع کند. هرچه تعداد دانشجو (کاربر) بیشتر شود، زمان لازم برای توزیع برگه‌ها توسط استاد بیشتر می‌شود. اما در مدل **P2P**، استاد (سرور) علاوه بر توزیع، می‌تواند برگه‌ها را به دانشجویان اولیه بدهد تا آن‌ها نیز همزمان به همکلاسی‌هایشان برگه‌ها را برسانند (هم آپلود و هم دانلود). با این روش، حتی اگر تعداد دانشجوها دو برابر شود، زمان توزیع تقریباً ثابت می‌ماند.