این درسنامه بر اساس متن پیاده‌سازی شده از **جلسه دوازدهم** کلاس شبکه‌های کامپیوتری تنظیم شده است. مطالب به صورت ساختاریافته، دسته‌بندی شده و با تکیه بر تعاریف و توضیحات استاد ارائه گردیده است.

---

# لایه شبکه (Network Layer)
**موضوعات:** معرفی لایه شبکه، مسیریابی، ساختار هدر IP، آدرس‌دهی IPv4 و کلاس‌های IP

---

## ۱. معرفی لایه شبکه (Network Layer)
لایه شبکه (لایه سوم از پایین در مدل OSI) به عنوان **هسته شبکه (Core)** شناخته می‌شود. تفاوت اصلی این لایه با لایه انتقال (Transport) در نوع ارتباط است:
*   **لایه انتقال:** مسئول ارتباط بین فرآیندها (Process-to-Process) است.
*   **لایه شبکه:** مسئول ارتباط بین میزبان‌ها (Host-to-Host) است.

### تعریف و وظیفه
وظیفه لایه شبکه در اینترنت هنگامی شروع می‌شود که بخواهیم بین **میزبان‌های مختلف** یا **شبکه‌های مختلف** ارتباط برقرار کنیم.
هنگامی که بسته‌ای روی شبکه منتشر می‌شود، باید مکانیزمی برای هدایت بسته از مبدأ به مقصد وجود داشته باشد؛ به این عمل **مسیریابی (Routing)** گفته می‌شود.

---

## ۲. مفاهیم پایه و سخت‌افزار

### الف) مسیریاب (Router)
روتر دستگاهی است با تعدادی ورودی و خروجی که بسته‌های اطلاعاتی (داده) را از ورودی دریافت کرده و به سمت خروجی مناسب هدایت می‌کند.

### ب) پروتکل IP (Internet Protocol)
قراردادی است برای جابه‌جایی بسته‌های داده، مدیریت و سازماندهی مسیریابی صحیح بسته‌ها از مبدأ به مقصد.

### ج) آدرس IP
آدرسی جهانی و منحصر‌به‌فرد برای مشخص کردن هر دیوایس یا سیستم در شبکه است که **فارغ از ساختار شبکه** می‌باشد. (یعنی مستقل از معماری شبکه، چیدمان روترها و اتصالات است).

### د) دیتاگرام (Datagram)
به بسته‌های داده در لایه شبکه، **دیتاگرام** گفته می‌شود. (یادآوری: نام بسته در هر لایه تغییر می‌کند؛ در این لایه دیتاگرام نام دارد).

---

## ۳. ساختار هدر IP (نسخه ۴)
در لایه شبکه، یک هدر (سرایند) به داده اضافه می‌شود. هدر IPv4 یک هدر **۳۲ بیتی** است (عرض سطرها ۳۲ بیت است). جزئیات فیلدهای هدر به شرح زیر است:

### سطر اول
1.  **Version (نسخه):** ۴ بیت. مشخص‌کننده نسخه پروتکل IP است (معمولاً ۴ برای IPv4 یا ۶ برای IPv6).
2.  **Header Length (طول هدر):** ۴ بیت. مشخص‌کننده طول سرایند بسته است.
    *   نکته: چون مبنای سرایند ۳۲ بیتی است، عدد ۵ در این فیلد قرار می‌گیرد ($5 \times 4 bytes = 20 bytes$).
3.  **Type of Service (نوع سرویس):** ۸ بیت. مشخص‌کننده درخواست سرویس ویژه‌ای توسط میزبان در شبکه است (مانند اولویت، تأخیر کم، قابلیت اطمینان و ...).
4.  **Total Length (طول کل):** ۱۶ بیت. طول کل بسته IP (شامل هدر + داده) را نشان می‌دهد.
    *   حداکثر طول بسته: $2^{16}$ بیت (تقریباً ۶۵,۵۳۶ بایت).

### سطر دوم (مربوط به قطعه‌قطعه شدن یا Fragmentation)
زمانی که بسته بزرگتر از حد مجاز شبکه باشد، قطعه‌قطعه می‌شود.
1.  **Identification (شناسه):** ۱۶ بیت. مشخص‌کننده شماره یک بسته در صورتی که قطعه‌قطعه شده باشد (تا در مقصد بازسازی شود).
2.  **Flags (پرچم‌ها):** ۳ بیت.
    *   بیت اول: **Reserved** (رزرو شده، استفاده نمی‌شود).
    *   بیت دوم (**DF - Don't Fragment**): اگر **۱** باشد، هیچ مسیریابی اجازه قطعه‌قطعه کردن بسته را ندارد. اگر **۰** باشد، اجازه دارد.
    *   بیت سوم (**MF - More Fragments**): اگر **۱** باشد، یعنی قطعات دیگری در راه است (وجود قطعات بعدی تأیید می‌شود). اگر **۰** باشد، یعنی این آخرین قطعه است.
3.  **Fragment Offset (جابجایی قطعه):** ۱۳ بیت. نشان‌دهنده شماره ترتیب هر قطعه از داده شکسته شده است.

### سطر سوم
1.  **TTL (Time To Live - طول عمر):** ۸ بیت. مشخص‌کننده طول عمر بسته است تا در شبکه سرگردان نشود.
    *   حداکثر عدد: $2^8 - 1 = 255$.
    *   در هر روتر یک واحد از این عدد کم می‌شود و اگر به صفر برسد، بسته حذف می‌شود.
2.  **Protocol (پروتکل):** ۸ بیت. نشان‌دهنده شماره پروتکل لایه بالاتر است (مثلاً مشخص می‌کند داده مربوط به TCP است یا UDP).
3.  **Header Checksum:** ۱۶ بیت. برای کشف خطای احتمالی در **هدر** بسته IP استفاده می‌شود.

### سطرهای بعدی
1.  **Source IP Address:** ۳۲ بیت. آدرس IP مبدأ.
2.  **Destination IP Address:** ۳۲ بیت. آدرس IP مقصد.
3.  **Options (اختیاری):** متغیر. اطلاعات اضافه (مثل ویژگی‌های روتر، شماره روتر و ...) که می‌تواند خالی باشد.

---

## ۴. آدرس‌دهی IPv4 و کلاس‌های IP
آدرس IPv4 یک عدد **۳۲ بیتی** است که شامل ۴ قسمت ۸ بیتی (Octet) می‌شود و با نقطه از هم جدا می‌شوند (مبنای دهدهی).
*   مثال: `192.168.1.1`
*   آدرس IP دارای دو بخش اصلی است: **NetID** (شناسه شبکه) و **HostID** (شناسه میزبان).

برای مدیریت بهتر، آدرس‌ها به ۵ کلاس تقسیم شده‌اند:

### کلاس A
*   **بیت‌های با ارزش (چپ):** همیشه با `0` شروع می‌شود.
*   **ساختار:** ۸ بیت اول **NetID** و ۲۴ بیت بعدی **HostID** است.
*   **فرمت:** `0xxxxxxx.Host.Host.Host`

### کلاس B
*   **بیت‌های با ارزش:** همیشه با `10` شروع می‌شود.
*   **ساختار:** ۱۴ بیت **NetID** و ۱۶ بیت **HostID** است (دو بخش اول شبکه، دو بخش دوم میزبان).
*   **فرمت:** `10xxxxxx.xxxxxxxx.Host.Host`

### کلاس C (معروف‌ترین کلاس)
*   **بیت‌های با ارزش:** همیشه با `110` شروع می‌شود.
*   **ساختار:** ۲۱ بیت **NetID** و ۸ بیت **HostID** است (سه بخش اول شبکه، بخش آخر میزبان).
*   **فرمت:** `110xxxxx.xxxxxxxx.xxxxxxxx.Host`

### کلاس D (Multicast)
*   **بیت‌های با ارزش:** با `1110` شروع می‌شود.
*   **کاربرد:** برای عملیات چندپخشی (Multicast) استفاده می‌شود.

### کلاس E (Reserved)
*   **بیت‌های با ارزش:** با `1111` شروع می‌شود.
*   **کاربرد:** برای آدرس‌های خاص و مقاصد تحقیقاتی رزرو شده است.

---

## ۵. آدرس‌های خاص در شبکه (Special IP Addresses)
۵ گروه آدرس وجود دارد که نمی‌توان از آن‌ها برای آدرس‌دهی معمول به سیستم‌ها استفاده کرد:

1.  **آدرس `0.0.0.0` (تمام بیت‌ها صفر):**
    *   زمانی استفاده می‌شود که سیستم از IP خودش مطلع نباشد. به طور پیش‌فرض این آدرس را انتخاب می‌کند.

2.  **آدرس `0.HostID` (بخش شبکه صفر است):**
    *   زمانی که سیستم (Host) آدرس شبکه‌ای که به آن تعلق دارد را نمی‌داند (NetID را ندارد)، اما HostID را می‌داند.

3.  **آدرس `NetID.255` (بخش میزبان تماماً یک):**
    *   **Directed Broadcast:** جهت ارسال پیام فراگیر به تمام سیستم‌های یک شبکه خاص که فرستنده **عضو آن شبکه نیست**.

4.  **آدرس `255.255.255.255` (تمام بیت‌ها یک):**
    *   **Limited Broadcast:** جهت ارسال پیام فراگیر به تمام سیستم‌های شبکه‌ای که فرستنده **عضو آن است** (پخش همگانی در شبکه محلی).

5.  **آدرس `127.x.x.x` (Loopback):**
    *   معروف‌ترین آن `127.0.0.1` است.
    *   به عنوان آدرس بازگشت (Loopback) شناخته می‌شود.
    *   **کاربرد:** برای اشکال‌زدایی (Debugging) و تست کارت شبکه استفاده می‌شود (داده از سیستم خارج نمی‌شود و به خود سیستم برمی‌گردد).

---

## ۶. مقایسه IPv4 و IPv6

### وضعیت IPv4
*   می‌تواند حدود **۴ میلیارد** ($2^{32}$) آدرس را ذخیره کند.
*   همچنان بالای ۹۰٪ ترافیک جهان را به خود اختصاص داده است.
*   پروتکلی ساده است و آدرس‌دهی در آن آسان است.

### ظهور IPv6
*   به دلیل افزایش انفجاری سیستم‌ها در شبکه (موبایل، لپ‌تاپ، اینترنت اشیاء و ...)، فضای آدرس‌دهی IPv4 رو به اتمام رفت.
*   **IPv6** با قابلیت‌های بیشتر و فضای آدرس‌دهی بسیار وسیع‌تر (سیستم هگزادسیمال ۱۲۸ بیتی) معرفی شد تا محدودیت تعداد آدرس‌ها را رفع کند.
