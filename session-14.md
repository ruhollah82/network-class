## ۱. پروتکل‌های مدیریت آدرس‌دهی و ترجمه

### الف) پروتکل DHCP (پیکربندی پویای میزبان)
*   **نام کامل:** Dynamic Host Configuration Protocol
*   **تعریف:** پروتکلی است که وظیفه اختصاص دادن آدرس IP و تنظیمات شبکه (مثل Subnet Mask, Gateway) را به صورت **خودکار** به میزبان‌ها (Hosts) بر عهده دارد.
*   **معماری:** این پروتکل از مدل **Client-Server** (مشتری-سرویس‌دهنده) تبعیت می‌کند.
    *   **DHCP Server:** آدرس‌های IP و تنظیمات را مدیریت و نگهداری می‌کند.
    *   **DHCP Client:** دستگاهی (رایانه، موبایل و ...) که به شبکه متصل شده و درخواست IP می‌کند.
*   **نحوه عملکرد (کلی):** کلاینت در شبکه فریاد می‌زند (Discover)، سرور پیشنهاد می‌دهد (Offer)، کلاینت درخواست می‌کند (Request) و سرور تأیید می‌کند (Ack).
*   **مزایا:**
    1.  **سهولت در مدیریت:** نیاز به آدرس‌دهی دستی تک‌تک سیستم‌ها نیست.
    2.  **کاهش خطا:** جلوگیری از تداخل IP (IP Conflict) یا خطای انسانی.
    3.  **انعطاف‌پذیری:** همکاری خوب با سایر پروتکل‌ها (مثل DNS).
*   **معایب:**
    1.  **ریسک امنیتی:** هر دستگاهی به شبکه وصل شود IP می‌گیرد (مگر کنترل شود).
    2.  **وابستگی به سرور:** اگر سرور DHCP از کار بیفتد، سیستم‌های جدید نمی‌توانند به شبکه متصل شوند (Single Point of Failure).

### ب) پروتکل NAT (ترجمه آدرس شبکه)
*   **نام کامل:** Network Address Translation
*   **تعریف:** روشی هوشمندانه که در آن تمامی دستگاه‌های یک شبکه داخلی (Private) از طریق یک آدرس IP عمومی (Public) مشترک با دنیای اینترنت ارتباط برقرار می‌کنند.
*   **نحوه عملکرد:** روتر مرزی، آدرس IP خصوصی بسته را با IP عمومی خود جایگزین می‌کند و جدولی برای بازگرداندن پاسخ‌ها نگه می‌دارد.
*   **مزایا:**
    1.  **صرفه‌جویی در IP:** کاهش مصرف آدرس‌های IPv4 (یک IP عمومی برای کل سازمان کافی است).
    2.  **افزایش امنیت:** مخفی ماندن ساختار آدرس‌دهی داخلی و IPهای خصوصی از دید شبکه خارجی.
    3.  **مدیریت ساده‌تر:** تغییر ISP یا IP عمومی نیازی به تغییر IP تمام سیستم‌های داخلی ندارد.
*   **معایب:**
    1.  **محدودیت در ارتباطات:** برخی ارتباطات مستقیم (End-to-End) را دشوار می‌کند.
    2.  **افزایش تأخیر (Latency):** فرآیند ترجمه آدرس زمان‌بر است.
    3.  **عدم سازگاری:** برخی پروتکل‌های امنیتی (مثل IPSec) ممکن است با NAT مشکل داشته باشند.

#### انواع NAT
1.  **Static NAT (ایستا):** نگاشت یک‌به‌یک و ثابت بین یک IP خصوصی و یک IP عمومی. (معمولاً برای سرورهای وب استفاده می‌شود که نیاز به دسترسی دائم از بیرون دارند).
2.  **Dynamic NAT (پویا):** استفاده از یک "استخر" (Pool) از IPهای عمومی. به هر کلاینت به صورت موقت یک IP عمومی آزاد اختصاص داده می‌شود.
3.  **NAT Overload (PAT - Port Address Translation):** رایج‌ترین نوع (در مودم‌های خانگی). تمام کاربران از **یک** IP عمومی استفاده می‌کنند، اما با **پورت‌های** مختلف تفکیک می‌شوند.
4.  **Symmetric NAT:** برای هر مقصد متفاوت، یک نگاشت متفاوت ایجاد می‌کند. (امنیت بالا، اما مشکل در اشتراک فایل P2P و ارتباطات مستقیم).

---

## ۲. مبانی مسیریابی (Routing)
وظیفه اصلی لایه شبکه، هدایت بسته‌ها (Packets) از مبدأ به مقصد است.

### تعاریف کلیدی (مرور)
*   **مسیریاب (Router):** ابزاری که بسته‌ها را دریافت کرده و بر اساس **IP مقصد**، بهترین خروجی را انتخاب می‌کند.
*   **جدول مسیریابی (Routing Table):** جدولی در حافظه روتر که اطلاعات مسیرها (مقصد، پورت خروجی، هزینه مسیر) در آن ذخیره می‌شود.
*   **گام (Hop):** عبور بسته از یک روتر. (تعداد روترها در مسیر = تعداد Hop).
*   **ترافیک شبکه:** میانگین تعداد بسته‌های ارسالی روی یک لینک در واحد زمان.
*   **توپولوژی:** نقشه‌ی فیزیکی و نحوه اتصال روترها و لینک‌ها.

### ویژگی‌های یک الگوریتم مسیریابی خوب
1.  **صحت (Correctness):** بسته را سالم و درست به مقصد برساند.
2.  **سادگی (Simplicity):** پیچیدگی محاسباتی کم داشته باشد.
3.  **پایداری (Stability):** در برابر تغییرات کوچک یا نویز، رفتار پایداری داشته باشد.
4.  **بهینگی (Optimality):** بهترین مسیر (کوتاه‌ترین، سریع‌ترین یا کم‌هزینه‌ترین) را انتخاب کند.

---

## ۳. طبقه‌بندی الگوریتم‌های مسیریابی
الگوریتم‌ها از دو دیدگاه اصلی تقسیم‌بندی می‌شوند:

### دیدگاه اول: روش تصمیم‌گیری (هوشمندی)

#### ۱. الگوریتم‌های ایستا (Static / Non-adaptive)
*   **عملکرد:** مسیرها از قبل تعیین شده و ثابت هستند. توجهی به شرایط لحظه‌ای شبکه (ترافیک، خرابی) ندارند.
*   **جدول مسیریابی:** ثابت است (مگر دستی تغییر کند).
*   **مزایا:** سرعت بالا (چون محاسباتی ندارد)، سادگی.
*   **معایب:** اگر روتری در مسیر خراب شود، ارتباط قطع می‌شود (انعطاف‌پذیر نیست).

#### ۲. الگوریتم‌های پویا (Dynamic / Adaptive)
*   **عملکرد:** تصمیم‌گیری در لحظه بر اساس وضعیت فعلی شبکه (ترافیک، توپولوژی) انجام می‌شود.
*   **جدول مسیریابی:** به صورت دوره‌ای (مثلاً هر چند ثانیه) به‌روزرسانی (Update) می‌شود.
*   **مزایا:** انتخاب بهترین مسیر در لحظه، مقاومت در برابر خرابی (اگر مسیری بسته شود، مسیر جایگزین انتخاب می‌شود).
*   **معایب:** پیچیدگی بیشتر، ایجاد سربار ترافیکی (برای آپدیت‌ها) و تأخیر محاسباتی.

### دیدگاه دوم: نحوه جمع‌آوری اطلاعات

#### ۱. الگوریتم‌های متمرکز (Centralized / Global)
*   **مثال:** Link State (LS).
*   **ویژگی:** همه روترها اطلاعات **کامل** از کل شبکه (توپولوژی و هزینه تمام لینک‌ها) را دارند.
*   **نحوه کار:** هر روتر نقشه‌ای کامل از شبکه دارد و مسیر را محاسبه می‌کند.

#### ۲. الگوریتم‌های غیرمتمرکز (Decentralized)
*   **مثال:** Distance Vector (DV).
*   **ویژگی:** روترها فقط از وضعیت **همسایگان مستقیم** خود خبر دارند.
*   **نحوه کار:** اطلاعات به صورت تدریجی و فقط بین همسایه‌ها تبادل می‌شود. پیچیدگی کمتری نسبت به روش متمرکز دارد.

---

## ۴. نکته تکمیلی: فناوری Tunneling (تونل‌زنی)
این مبحث مربوط به فصل ۴ است که استاد در پایان جلسه به عنوان نکته تکمیلی بیان کردند.

*   **تعریف:** روشی برای ارسال داده‌های یک پروتکل از طریق پروتکل دیگر، با کپسوله‌کردن (Encapsulation) بسته در دل یک بسته دیگر.
*   **کاربرد اصلی ۱ (انتقال IPv6 از IPv4):**
    *   بسیاری از زیرساخت‌های اینترنت هنوز IPv4 هستند.
    *   برای ارتباط دو شبکه IPv6 از طریق یک بستر IPv4، بسته‌های IPv6 درون بسته‌های IPv4 قرار می‌گیرند (تونل زده می‌شود) تا از شبکه عبور کنند.
*   **کاربرد اصلی ۲ (VPN):**
    *   ایجاد یک شبکه خصوصی مجازی امن روی بستر ناامن اینترنت.
    *   داده‌ها رمزگذاری شده و از طریق تونل منتقل می‌شوند.
*   **مزایا:**
    *   اتصال شبکه‌هایی با ساختارهای متفاوت.
    *   افزایش امنیت و حریم خصوصی.
    *   دور زدن محدودیت‌های جغرافیایی یا شبکه‌ای.
